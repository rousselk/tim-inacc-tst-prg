Copy the 'test-tx-packet' folder in the 'examples' subfolder of your
RIOT OS installation.

You can then build the program the standard way:

  cd ${RIOTBASE}/examples/test-tx-packet
  make BOARD=<target>

... with 'target' equal to 'telosb' or 'z1', in the current version.

