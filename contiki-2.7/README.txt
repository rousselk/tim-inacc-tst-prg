Copy the 'test-tx-paquet' folder in the 'examples' subfolder of your
Contiki 2.7 installation.

You can then build the program the standard way:

  cd ${CONTIKI-2.7}/examples/test-tx-paquet
  make TARGET=<board>

... with 'board' equal to 'sky' or 'z1', in the current version.

